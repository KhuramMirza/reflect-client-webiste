export interface UserData {
  _id: string;
  name: string;
  email: string;
  profileImage: string;
  bio?: string;
  role: string;
}

export interface UserProps {
  _id: string;
  name: string;
  email: string;
  profileImage: string;
  bio?: string;
  role: string;
}
